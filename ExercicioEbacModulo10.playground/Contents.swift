// 1 - Apesar da Struct e da Class se assemelharem bastante, elas possuem algumas difereças, tais como: as Struct não herdam atributos ou metodos de outras structs ou classes, diferentemente da class que consegue herdar, outra diferença entre elas é a que as struct são do tipo valor e as classes de tipo referencia

// 2 - Os objetos podem ser classificados e agrupados, onde uma classe descreve todos os objetos de um mesmo tipo

// 3 - A classe é como se fosse a planta de uma casa, um molde, já os objetos são como essas classes já materializadas já com seus atributos qualificados
